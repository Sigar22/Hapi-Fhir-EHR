<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.2"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">

    <!-- Persistence Unit для вашего кастомного модуля -->
    <persistence-unit name="CUSTOM_PU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <class>ca.uhn.fhir.jpa.starter.custom.LoincCode</class> <!-- Явно указываем вашу сущность -->

        <properties>
            <!-- Свойства Hibernate будут взяты из application.yml, но здесь можно переопределить -->
            <!-- Например, если у вас другой ddl-auto для ваших таблиц -->
            <!-- <property name="hibernate.hbm2ddl.auto" value="update"/> -->
            <!-- <property name="hibernate.dialect" value="ca.uhn.fhir.jpa.model.dialect.HapiFhirPostgresDialect"/> -->
            <!-- <property name="hibernate.show_sql" value="false"/> -->
            <!-- <property name="hibernate.format_sql" value="false"/> -->
        </properties>
    </persistence-unit>

</persistence>